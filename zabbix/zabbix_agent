
nano docker-compose.yml

version: '3'
services:
  zabbix-agent:
    image: 'zabbix/zabbix-agent:latest'
    container_name: zabbix-agent
    restart: always
    environment:
      - ZBX_HOSTNAME=wiki
      - ZBX_SERVER_HOST=10.129.135.110
    ports:
      - "10050:10050"



docker-compose up -d
